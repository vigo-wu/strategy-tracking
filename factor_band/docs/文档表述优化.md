## 📝 任务卡片 1：全局“消灭黑话与口语”术语替换

* 指令目标：遍历并重写所有 md 文档，将带有重度口语和模糊代称的词汇替换为标准的量化与工程规范用词。
* 具体 TODO 列表：
* 替换工作流表述：将所有“同一回合 / 同一回合改”统一替换为“在同一次代码提交（Commit）中同步修改”。
    * 替换环境表述：将“现网文档 / 现网 md”统一替换为“主分支文档（Main Branch Docs）”；将代码和环境里的“现网”按场景替换为“主分支代码”或“交易终端实盘环境”。
    * 替换核心业务名词：将“读窗 / 结构窗”统一替换为“时间窗口（Window Size）/ 指标周期窗”；将“参数真源”统一替换为“唯一可信数据源（Single Source of Truth）”。
    * 替换 HACK 动作词：将“硬切 / 覆盖硬切”统一替换为“强制运行时覆盖（Runtime Overrides）”；将代码解释中的“吃参数”替换为“读取/接受参数”。
    * 替换并发名词：将“探针 / init 探针”统一替换为“参数指纹预检（Dummy Context Check）”；将“一层全局 walk 池”统一替换为“全局单层多进程池”。

------------------------------
## 📝 任务卡片 2：重构【HlBand 契约与文档同步规范】（原 File 1, 3）

* 指令目标：清晰梳理参数变更时文档必须同步的硬性 Checklist，消除歧义。
* 具体 TODO 列表：
* 重写触发场景：将原“触发（任一即做）”部分，改为结构清晰的「当以下代码发生变动时，必须同步更新文档」触发列表，并将函数名（如 _calc_macd 必传窗）加上代码块标记。
    * 规范四槽 AST 表述：将“四槽 AST”清晰解释为“买入（Entry）、卖出（Exit）、加仓（Scale）、风控（Risk）四个核心模块的条件抽象语法树”。
    * 显性化 Agent 路由规则：将 File 3 的 Skill 选用逻辑重写为「动手前必读规范」，将原本口语化的拒绝词（如“不是同一条路”、“不要用错”）改为严谨的「一票否决/禁止行为」清单。

------------------------------
## 📝 任务卡片 3：重构【QMT 共用模块与工程约束】（原 File 2）

* 指令目标：卡死多实例实盘环境下的工程防错，严防数据污染与逻辑混接。
* 具体 TODO 列表：
* 拆解“串标的”灾难：将“多实盘实例共用同一 STATE JSON（串标的）”重写为：“【重大风险警告】 多个实盘运行实例严禁共用同一个状态文件（STATE JSON），否则会导致不同交易标的（Stock）的持仓与订单数据发生相互覆盖和串线污染。”
    * 规范单仓/双浮仓逻辑：将“单仓/双浮仓 MODULE_ORDER 混用（错接...）”重写为行为规范，明确指出混合调用自研浮仓和单仓订单库的技术后果。
    * 重写禁止手改规则：将禁止手改终端 GBK 和 XML 的规则，梳理为标准的规范化流程——即只能修改策略片段，然后统一通过 _deploy_qmt_gbk.py 拼接部署。

------------------------------
## 📝 任务卡片 4：重构【HlBand 因子与指标扩展规范】（原 File 4, 5）

* 指令目标：理清 LEAVES（登记表）、AST（四槽引用）以及指标（Indicators）之间的互斥与共生关系，补充防错上下文。
* 具体 TODO 列表：
* 重写核心词表定义：将因子、指标、叶子的定义重写。明确“指标”是纯粹的数学计算序列（不带多空立场，不读配置文件）；“因子”是返回布尔值的原子信号；“叶子”是四槽条件树里的引用代号。
    * 高亮类型防错警告：在登记表（LEAVES）参数类型部分，将 2 ≠ 2.0（浮点与整型错位）、tiers 必须用 list 严禁 tuple 提取出来，做成独立的、高亮的 【类型安全防错警告】 模块。
    * 重构流程 Checklist：将 A、B、C、D 四个操作流程（登记、启用、加减、除名）中的“吃序列”、“专线”等程序员黑话去掉，改成标准的步骤说明书。

------------------------------
## 📝 任务卡片 5：重构【本地网格确认与实盘评估】（原 File 6, 7, 8, 9）

* 指令目标：划清 MAE 反事实与真实 local_bt 的可信度红线，规范新主题接入契约与自动化流程。
* 具体 TODO 列表：
* MAE 证伪原理解释：将“放宽/关闭导致高估”重写为逻辑清晰的因果分析（即：静态反事实无法预测持仓延长后，对后续加仓、其他卖点触发以及后续开仓带来的路径改动，因此极易把收益符号估反）。
    * 规范新主题接入契约：将 File 8 的新主题接入部分，整理成标准的「新策略接入网格 5 大核心增量 Checklist」（包括 overrides 注入、隔离产物目录规范、日志指纹打印标准、主样本 walk 限制等）。
    * 重写过门与选参机制：将选参推荐的“三关淘汰赛”逻辑（绝对合格红线过滤 -> 统计口径统一 -> 卡玛比率 OOS 排序）重新模块化梳理，明确空仓年计入分母的几何年化计算公式。
    * 规范 UI 暂停与死锁控制：清晰重写进程池（禁止嵌套）和 UI 安全暂停逻辑，说明如何通过操作系统命令行双重核对 pid，防止因残留进程未退出而导致的目录误删。

