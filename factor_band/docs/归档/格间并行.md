# 格间并行（local_bt 网格）

本文是早期「一格一个 worker、格内钉死 1、自动上限 8」方案的草稿，**不要按正文旧表实现**。

现行调度是 **一层全局 walk 池**：探针在主进程串行，通过后把各格 walk 铺平进同一 `ProcessPool`。Queue 只能 initializer 继承、失败分流、Windows spawn 下 `app.py` 跳过 `st.*`，这些约束仍有效。

经验与现行口径见 [并行经验.md](并行经验.md)。契约见 `.cursor/skills/qmt-local-bt-grid`。
