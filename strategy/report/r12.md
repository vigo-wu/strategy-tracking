# v0.9 撤回 v0.8 入口放宽

对照 `r11.md`：v0.8 从 +14.8% 掉到 +7.3%。多开的单几乎都在方向过滤被拆掉之后。

| 因子 | v0.8 | v0.9 |
| :--- | :--- | :--- |
| 小时趋势 | 只要 `hMA20>hMA60` | **恢复** 快线非下行 |
| 15m 趋势 | MA60 可微下 | **恢复** MA60 当根不下行 |
| 触线 | 0.8% | **0.4%** |
| 缩量 | 锤子/反包免；bounce 1.10 | **一律 <85% 均量** |
| stall 当日冷却 | 已关 | **仍关**（2/11 bounce 优于次日隔夜锤） |

预期：应回到接近 v0.7 的 14 笔 / +7,000 一带；2 月可能是 stall 后再 bounce（-317）而不是次日锤（-441）。**不是冲 30% 的一刀**，是先把被 v0.8 打穿的边修回来。
