# v0.7 收紧 trend_break（对照 v0.6）

v0.6 合计从 +7,517 掉到 +5,658。心跳日志只看到 7/17 11:00 `vol_skip`，13:30 才 `trend_skip`，把 7/16 止盈 +944 砍成 −573。4/17 仅 −0.06% 也出了，4/9 giveback +474 没了。

**不删 `trend_break`**（9/12 从 −352 收到 −128 仍有用），改成和 stall 一样：曾启动过的单不用这层。

| 因子 | v0.6 | v0.7 |
| :--- | :--- | :--- |
| trend_break | 浮亏 + 趋势坏即出 | 另需 **当前浮亏 ≤ −0.4%**，且 **持仓期最大浮盈 < 0.1%** |
| stall 当日冷却 | 禁 hammer/bounce | **保留** |

7/16 当日已有约 +0.19%，不会再被 trend_break；4/9 中途曾 +1%，也不会。9/12 几乎没启动，仍应提前出。

对照：7/16 应回到止盈；4/9 应能拿到 giveback；9/12 仍可以是 `trend_break`。不要再放宽 bounce。
