# v1.1 恢复硬止盈（对照 v1.0）

v1.0 从 +15.0% 掉到 +12.8%，见 `r15.md`。出场回到 v0.9：

| 因子 | v1.0 | v1.1 |
| :--- | :--- | :--- |
| 离开均线硬止盈 | 关 | **开**（面板可关） |
| 回吐 | 1.2% | **0.8%** |

代码里仍留着 `TAKE_PROFIT_HARD` / 4% 收紧回吐，默认与 v0.9 同一条成交路径。不必为对数字再跑一轮；若要确认，日志应出现 `Ma15 v1.1 init` 且 `hard_tp= True`。
